# TopTycoon 专项分析配置
# 时间范围: 2025年9月-12月

# 游戏配置（只配置TopTycoon）
games:
  - name: "TopTycoon"
    appstore_id: ""  # 不采集iOS
    playstore_id: ""  # 需要填入Google Play ID
    platforms: ["android"]  # 只采集Google Play

# 采集配置
scraper:
  time_range_days: 120  # 约4个月（9-12月）
  max_reviews_per_game: 10000  # 增加采集数量
  delay_between_requests: 2  # 请求间隔（秒）
  retry_times: 3  # 重试次数
  # 地区配置 - 只采集美国区
  regions:
    - name: "美国"
      lang: "en"
      country: "us"

# 数据存储
storage:
  raw_data_format: "json"
  processed_data_format: "csv"
  database_path: "data/reviews.db"

